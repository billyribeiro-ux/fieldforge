# FieldForge Web — Environment Variables
# Copy to .env and configure

# API URL (used by server-side load functions and client-side fetch)
PUBLIC_API_URL=http://localhost:8080/api/v1

# App origin (used by SvelteKit for CSRF protection)
ORIGIN=http://localhost:5173

# Node environment
NODE_ENV=development
